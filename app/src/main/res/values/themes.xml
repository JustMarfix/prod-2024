<resources>

    <style name="Theme.SMManager.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="android:animationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.SMManager</item>
    </style>

    <style name="Base.Theme.SMManager" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.SMManager" parent="Base.Theme.SMManager" />
</resources>